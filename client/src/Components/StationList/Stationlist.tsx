import { Box } from "@mui/material";

const Stationlist = () => {
  return (
    <Box
      sx={{
        minHeight: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        margin: "20px",
      }}
    ></Box>
  );
};

export default Stationlist;
